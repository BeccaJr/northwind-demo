version: 2

models:
  - name: fact_vendas
    description: Essa é a fato de vendas
    columns:
      - name: sk_venda
        description: Essa é a chave surrogate key da tabela
        tests:
          - unique
          - not_null